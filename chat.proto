syntax = "proto3";

package chat;

service Chat {
  rpc SendMessage (ChatMessage) returns (Empty);
  rpc ReceiveMessage(Empty) returns (stream ChatMessage);
}

message ChatMessage {
  string name = 1;
  string message = 2;
}

message Empty {}

